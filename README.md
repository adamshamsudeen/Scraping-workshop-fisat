# Webscrape Fisat faculty page

## Requirements:

python3, beautifulsoup, requests

## Installation:

virutalenv -p /usr/bin/python3 vir

source ./vir/bin/activate

## Inside virtualenv:

pip install -r req.txt

python scrape.py
